#include <stdio.h>
/**
 * main - prints alpha in upper and lower case
 * Return: always 0
 */
{
char x;
for (x = 'a'; x <= 'z'; x++)
putchar(x);
for (x = 'A'; cx <= 'Z'; x++)
putchar(x);
putchar('\n');
return (0);
}
